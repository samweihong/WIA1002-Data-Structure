package tutorial2;

public class Q10 {
    // When the compiler encounters a generic class, interface, or method with a bound type parameter,
    // such as <T extends Number> or <E extends Comparable>, it replaces all occurrences of the type parameter with what type?

    // The bounded type.
}
